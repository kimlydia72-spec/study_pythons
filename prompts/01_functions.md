# Prompts

```
{
  "prompt": "주어진 Python 코드 골격을 완성하여 두 숫자에 대한 사칙연산(덧셈, 뺄셈, 곱셈, 나눗셈)을 수행하는 `calculate_all` 함수를 구현하세요.\n\n## 📋 구현 조건\n\n1.  **함수 정의:** `def calculate_all(num1, num2):` 내부에 사칙연산을 구현합니다.\n2.  **반환 값:** 함수는 **(덧셈 결과, 뺄셈 결과, 곱셈 결과, 나눗셈 결과)**의 **튜플**을 반환해야 합니다.\n3.  **나눗셈 처리:** 만약 두 번째 숫자(`num2`)가 **0**이어서 0으로 나누는 연산이 발생하는 경우, 나눗셈 결과 대신 **문자열 \"division_error\"**를 반환해야 합니다. (다른 연산의 결과는 정상적으로 계산되어야 합니다.)\n4.  **변수명:** 주어진 코드 골격의 변수명(소문자 + _ 조합)을 따르세요.\n\n## 📝 문제 코드 골격\n\n```python\ndef calculate_all(num1, num2):\n    # 여기에 사칙연산 구현\n    # return (덧셈, 뺄셈, 곱셈, 나눗셈)\n    pass\n\n\n# 테스트 리스트 (10개)\ntest_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]\ntest_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]\n\n# 테스트 실행 코드는 수정하지 마세요.\nfor i in range(10):\n    a = test_a[i]\n    b = test_b[i]\n    result = calculate_all(a, b)\n    print(f\"{a}, {b} => {result}\")\n```\n\n## 💡 예시 출력\n\n(참고: 예시 출력은 `test_a[4]`와 `test_b[4]`인 7과 0에 대한 출력을 포함해야 합니다.)\n\n```\n...\n7, 0 => (7, 7, 0, 'division_error')\n...\n```\n\n## 🚀 당신의 임무\n\n위의 `calculate_all` 함수를 완성하고 전체 코드를 제출하세요.",
  "title": "Python 함수 구현: 두 숫자에 대한 사칙연산 및 0으로 나누기 예외 처리",
  "language": "python",
  "tags": ["function", "arithmetic", "exception-handling", "list", "tuple"]
}
```
# 프롬프트의 코드
```
def calculate_all(num1, num2):
    # 덧셈
    add_result = num1 + num2
    # 뺄셈
    subtract_result = num1 - num2
    # 곱셈
    multiply_result = num1 * num2
    
    # 나눗셈 (0으로 나누기 처리)
    if num2 == 0:
        division_result = "division_error"
    else:
        division_result = num1 / num2
        
    # 결과 튜플 반환
    return (add_result, subtract_result, multiply_result, division_result)


# 테스트 리스트 (10개)
test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]
test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]

# 테스트 실행 코드는 수정하지 마세요.
for i in range(10):
    a = test_a[i]
    b = test_b[i]
    result = calculate_all(a, b)
    print(f"{a}, {b} => {result}")
```
# Outputs (프롬프트 실행 후, 코드에 따른 값)

```
10, 5 => (15, 5, 50, 2.0)
25, 5 => (30, 20, 125, 5.0)
40, 8 => (48, 32, 320, 5.0)
12, 3 => (15, 9, 36, 4.0)
7, 0 => (7, 7, 0, 'division_error')
9, 3 => (12, 6, 27, 3.0)
16, 2 => (18, 14, 32, 8.0)
100, 4 => (104, 96, 400, 25.0)
3, 9 => (12, -6, 27, 0.3333333333333333)
81, 9 => (90, 72, 729, 9.0)
```
